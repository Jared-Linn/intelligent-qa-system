# 智能问答系统 — 总依赖
# 按需安装: pip install -r requirements.txt
# 或分别安装子目录的 requirements.txt

# ── 公共 ──
pyyaml>=6.0
jieba>=0.42.1
numpy>=1.24.0

# ── Web ──
fastapi>=0.100.0
uvicorn>=0.23.0
jinja2>=3.0.0

# ── 检索式 ──
scikit-learn>=1.2.0
scipy>=1.9.0

# ── 生成式（可选，训练千问时安装） ──
# torch>=2.0.0
# transformers>=4.40.0
# datasets>=2.14.0
# accelerate>=0.28.0
# peft>=0.10.0
# trl>=0.8.0

# RAR 解压支持（生成式 LoRA 上传）
rarfile>=4.0
